# the non-convergence warning names the knobs that move the outcome

    Code
      cat(conditionMessage(w))
    Output
      The rotation did not converge within 1 iteration.
      i In `rotate_control()`, increase `maxit` for more iterations, or `precision` for a weaker tolerance.

