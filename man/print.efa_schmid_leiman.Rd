% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.efa_schmid_leiman.R
\name{print.efa_schmid_leiman}
\alias{print.efa_schmid_leiman}
\alias{format.efa_schmid_leiman}
\title{Print and format an efa_schmid_leiman object}
\usage{
\method{print}{efa_schmid_leiman}(x, ...)

\method{format}{efa_schmid_leiman}(x, ...)
}
\arguments{
\item{x}{An object of class \code{efa_schmid_leiman} (output from
\code{\link[=efa_schmid_leiman]{efa_schmid_leiman()}}).}

\item{...}{Not used; for consistency with the generic.}
}
\value{
\code{print()} returns its argument \code{x} invisibly. \code{format()} returns a
character vector with the report lines (styled to the active console theme;
plain when colours are disabled).
}
\description{
\code{print()} shows a summarised output of the \code{\link[=efa_schmid_leiman]{efa_schmid_leiman()}} function: a
model header (when the settings are available), the Schmid-Leiman loading
matrix, and the variances accounted for. \code{format()} assembles the same report
and returns it as a character vector; \code{print()} is \code{cat(format(x), sep = "\\n")}.
The lines follow the active console theme, so they are plain when colours are
disabled (for example when captured into a file or stripped with
\code{\link[cli:ansi_strip]{cli::ansi_strip()}}).
}
\examples{
EFA_mod <- efa_fit(test_models$baseline$cormat, N = 500, n_factors = 3,
                   estimator = "PAF", rotation = "promax")
sl_mod <- efa_schmid_leiman(EFA_mod, estimator = "PAF")
sl_mod

# format() returns the same lines as a character vector:
writeLines(format(sl_mod))

}
