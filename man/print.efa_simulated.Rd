% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.efa_simulated.R
\name{print.efa_simulated}
\alias{print.efa_simulated}
\alias{format.efa_simulated}
\title{Print and format an efa_simulated object}
\usage{
\method{print}{efa_simulated}(x, digits = 3, ...)

\method{format}{efa_simulated}(x, digits = 3, ...)
}
\arguments{
\item{x}{An object of class \code{efa_simulated} (output from \code{\link[=efa_simulate]{efa_simulate()}}).}

\item{digits}{Integer. The number of decimal places the reported fit values are rounded to.
Default is 3.}

\item{...}{Not used; for consistency with the generic.}
}
\value{
\code{print()} returns its argument \code{x} invisibly. \code{format()} returns a character vector
with the summary lines (styled to the active console theme; plain when colours are
disabled).
}
\description{
\code{print()} shows a compact summary of the data simulated by \code{\link[=efa_simulate]{efa_simulate()}}: how many
datasets were drawn and their dimensions, the marginal distribution, whether the data were
discretized into ordered categories or given missing values, and -- when model error was
injected -- the method with the target and achieved RMSEA and CFI. The simulated data
themselves live in the \code{data} element and the population correlation matrix in \code{population}.
\code{format()} returns the same summary as a character vector; \code{print()} is
\code{cat(format(x), sep = "\\n")}. The lines follow the active console theme, so they are plain
when colours are disabled.
}
\examples{
Lambda <- population_models$loadings$baseline
Phi <- population_models$phis_3$moderate
efa_simulate(N = 500, Lambda = Lambda, Phi = Phi, target_rmsea = 0.05, seed = 42)

}
\seealso{
Other data simulation:
\code{\link[=efa_simulate]{efa_simulate()}}
}
\concept{data simulation}
