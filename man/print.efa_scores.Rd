% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/efa_scores.R
\name{print.efa_scores}
\alias{print.efa_scores}
\alias{format.efa_scores}
\alias{summary.efa_scores}
\alias{print.summary.efa_scores}
\alias{format.summary.efa_scores}
\title{Print and format an efa_scores object}
\usage{
\method{print}{efa_scores}(x, digits = 3, ...)

\method{format}{efa_scores}(x, digits = 3, ...)

\method{summary}{efa_scores}(object, digits = 3, ...)

\method{print}{summary.efa_scores}(x, ...)

\method{format}{summary.efa_scores}(x, digits = x$opts$digits, ...)
}
\arguments{
\item{x, object}{An object of class \code{efa_scores}; for the \code{summary.efa_scores}
methods, the object returned by \code{\link[=summary]{summary()}}.}

\item{digits}{numeric. Number of decimal places for the printed tables.
Default is 3.}

\item{...}{Not used; for consistency with the generics.}
}
\value{
\code{print()} and the print method for \code{summary.efa_scores} objects return
their argument invisibly. \code{format()} returns a character vector with the
report lines (styled to the active console theme; plain when colours are
disabled). \code{summary()} returns an object of class \code{summary.efa_scores}.
}
\description{
\code{print()} shows a concise overview of an \code{\link[=efa_scores]{efa_scores()}} result: a header naming
the method and whether factor scores were computed, and the per-factor
determinacy table (determinacy, squared determinacy, and Guttman index).
\code{\link[=summary]{summary()}} returns a \code{summary.efa_scores} object whose print method adds the
full factor-weight matrix, the score validity/univocality matrix, and the score
intercorrelations. \code{format()} assembles the same report and returns it as a
character vector; \code{print()} is \code{cat(format(x), sep = "\\n")}. The lines follow
the active console theme, so they are plain when colours are disabled (for
example when captured into a file or stripped with \code{\link[cli:ansi_strip]{cli::ansi_strip()}}).
}
\examples{
efa <- EFA(test_models$baseline$cormat, n_factors = 3, N = 500,
           method = "PAF", rotation = "oblimin")
fs <- efa_scores(test_models$baseline$cormat, f = efa)
fs
summary(fs)

# format() returns the same lines as plain text:
writeLines(format(fs))

}
\seealso{
Other factor scoring:
\code{\link[=FACTOR_SCORES]{FACTOR_SCORES()}},
\code{\link[=efa_scores]{efa_scores()}}
}
\concept{factor scoring}
