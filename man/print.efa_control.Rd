% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{print.efa_control}
\alias{print.efa_control}
\alias{print.efa_estimate_control}
\alias{format.efa_estimate_control}
\alias{print.efa_rotate_control}
\alias{format.efa_rotate_control}
\title{Print and format a control object}
\usage{
\method{print}{efa_estimate_control}(x, ...)

\method{format}{efa_estimate_control}(x, ...)

\method{print}{efa_rotate_control}(x, ...)

\method{format}{efa_rotate_control}(x, ...)
}
\arguments{
\item{x}{A control object from \code{\link[=estimate_control]{estimate_control()}} or \code{\link[=rotate_control]{rotate_control()}}.}

\item{...}{Not used; for consistency with the generic.}
}
\value{
\code{print()} returns its argument \code{x} invisibly. \code{format()} returns a
character vector with the report lines.
}
\description{
\code{print()} shows the chosen \code{type} and each tuning knob, with an unset (\code{NA})
preset-driven knob marked as resolved from the \code{type} preset. \code{format()}
assembles the same report and returns it as a character vector; \code{print()} is
\code{cat(format(x), sep = "\\n")}. The lines follow the active console theme, so they
are plain when colours are disabled.
}
\examples{
est <- estimate_control(type = "SPSS")
est
writeLines(format(est))

}
\seealso{
\code{\link[=estimate_control]{estimate_control()}}, \code{\link[=rotate_control]{rotate_control()}}

Other Control functions:
\code{\link[=estimate_control]{estimate_control()}}
}
\concept{Control functions}
