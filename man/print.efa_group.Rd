% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.efa_group.R
\name{print.efa_group}
\alias{print.efa_group}
\alias{format.efa_group}
\title{Print and format a multigroup factor analysis}
\usage{
\method{print}{efa_group}(x, digits = 3, ...)

\method{format}{efa_group}(x, digits = 3, ...)
}
\arguments{
\item{x}{An object of class \code{efa_group} (output from \code{\link[=efa_group]{efa_group()}}).}

\item{digits}{Integer. The number of decimal places the reported values are rounded to.
Default is 3.}

\item{...}{Not used; for consistency with the generic.}
}
\value{
\code{print()} returns its argument \code{x} invisibly. \code{format()} returns a character
vector with the report lines (styled to the active console theme; plain when colours
are disabled).
}
\description{
\code{print()} turns an \code{\link[=efa_group]{efa_group()}} result into a sectioned report: a header recapping
the groups, the common number of factors, the estimator, the rotation, and the
alignment; a group-pair table of the matched Tucker congruences between the aligned
loadings; a per-pair summary of the cross-group loading differences (with the salient
and, when a bootstrap was run, the confidence-interval flags); and, when
\code{invariance = TRUE}, a group-pair by factor grid of the approximate-invariance verdicts.
\code{format()} assembles the same report and returns it as a character vector; \code{print()} is
\code{cat(format(x), sep = "\\n")}. The lines follow the active console theme, so they are
plain when colours are disabled (for example when captured into a file or stripped with
\code{\link[cli:ansi_strip]{cli::ansi_strip()}}). \code{print()} does not draw a plot; use \code{\link[=plot.efa_group]{plot.efa_group()}}.
}
\examples{
g <- rep(c("g1", "g2"), length.out = nrow(GRiPS_raw))
mg <- efa_group(GRiPS_raw, groups = g, n_factors = 1)
mg

# format() returns the same lines as plain text:
writeLines(format(mg))

}
\seealso{
Other factor analysis:
\code{\link[=efa_average]{efa_average()}},
\code{\link[=efa_fit]{efa_fit()}},
\code{\link[=efa_group]{efa_group()}},
\code{\link[=efa_mi]{efa_mi()}},
\code{\link[=plot.efa_group]{plot.efa_group()}}
}
\concept{factor analysis}
