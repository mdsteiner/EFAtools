% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.efa_group.R
\name{plot.efa_group}
\alias{plot.efa_group}
\title{Plot a multigroup factor analysis}
\usage{
\method{plot}{efa_group}(x, type = c("congruence", "differences"), ...)
}
\arguments{
\item{x}{An object of class \code{efa_group} (output from \code{\link[=efa_group]{efa_group()}}).}

\item{type}{character. Which plot to draw: \code{"congruence"} (per-factor congruence with
confidence intervals) or \code{"differences"} (a per-item loading-difference heatmap).}

\item{...}{Not used; for consistency with the generic.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Two views of an \code{\link[=efa_group]{efa_group()}} result, selected by \code{type}:
}
\details{
\itemize{
\item \code{"congruence"} (the default) plots the matched Tucker congruence of each factor
between every group pair, with a percentile bootstrap confidence interval when one
was computed (\code{b_boot > 0}). The Lorenzo-Seva and ten Berge (2006) reference bands
(\code{.95} "equal", \code{.85} "fair") are drawn so a factor's cross-group similarity can be
read against them at a glance.
\item \code{"differences"} draws a heatmap of the signed cross-group loading differences (item
by factor, one panel per group pair). Cells whose absolute difference reaches the
salience threshold \code{delta} are outlined.
}
}
\examples{
g <- rep(c("g1", "g2"), length.out = nrow(GRiPS_raw))
mg <- efa_group(GRiPS_raw, groups = g, n_factors = 1)

# Per-factor congruence against the Lorenzo-Seva & ten Berge bands
plot(mg)

# Per-item cross-group loading-difference heatmap
plot(mg, type = "differences")
}
\references{
Lorenzo-Seva, U., and ten Berge, J. M. F. (2006). Tucker's congruence coefficient as a
meaningful index of factor similarity. \emph{Methodology}, 2, 57-64.
doi: 10.1027/1614-2241.2.2.57
}
\seealso{
Other factor analysis:
\code{\link[=efa_group]{efa_group()}},
\code{\link[=print.efa_group]{print.efa_group()}}
}
\concept{factor analysis}
